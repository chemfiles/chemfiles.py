name: Documentation

on:
  push:
    branches: [master]
    tags: ["*"]
  pull_request:
    # Check all PR

jobs:
  build-and-publish:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: true
      - name: set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: install dependencies
        run: |
          pip install tox
      - name: build documentation
        run: |
          tox -e docs
          rm -rf docs/build/html/.doctrees/ docs/build/html/.buildinfo
          rm -rf docs/build/html/_static/bootswatch-2.3.2/ docs/build/html/_static/bootstrap-2.3.2/
          shopt -s extglob
          cd docs/build/html/_static/bootswatch-* && rm -rf !(flatly) && cd -
      - name: put documentation in the website
        run: |
          git clone https://github.com/$GITHUB_REPOSITORY --branch gh-pages gh-pages
          rm -rf gh-pages/.git

          REF_KIND=$(echo $GITHUB_REF | cut -d / -f2)
          if [[ "$REF_KIND" == "tags" ]]; then
              TAG=${GITHUB_REF#refs/tags/}
              mv docs/build/html/ gh-pages/$TAG
          else
              rm -rf gh-pages/latest
              mv docs/build/html/ gh-pages/latest
          fi
      - name: deploy to gh-pages
        if: github.event_name == 'push'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./gh-pages/
          force_orphan: true
